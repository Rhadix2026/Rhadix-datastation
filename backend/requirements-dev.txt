# Rhadix Uitvraag — ontwikkel- en testdependencies
# Installeer met: pip install -r requirements-dev.txt
# Productie-dependencies staan in requirements.txt

-r requirements.txt

# Test framework
pytest>=8.0
pytest-asyncio>=0.23
pytest-cov>=5.0

# HTTP-testclient (FastAPI integratietests)
httpx>=0.27.0
